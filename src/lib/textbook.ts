// Полноценная теория-учебник для каждого модуля
// Доступна через кнопку "Учебник" в плеере урока

export const textbook: Record<number, { title: string; content: string }> = {
  0: {
    title: "Модуль 0: Первый запуск — полный учебник",
    content: `# Ваша первая программа на Python

## Что такое программирование?

Программирование — это написание инструкций для компьютера. Компьютер выполняет их строго по порядку, сверху вниз. Он не умеет думать или догадываться — делает ровно то, что написано.

Программа на Python — это обычный текстовый файл с расширением .py. Вы пишете команды, а Python их выполняет.

## Команда print()

\`print()\` — самая первая команда, которую вы изучите. Она выводит текст на экран (в терминал).

### Синтаксис

\`\`\`python
print("Ваш текст здесь")
\`\`\`

Разберём по частям:
- \`print\` — имя команды (функции)
- \`(\` и \`)\` — круглые скобки, внутри них — что вывести
- \`"Ваш текст здесь"\` — текст в кавычках (строка)

### Правила

1. **Текст всегда в кавычках.** Можно использовать двойные \`"текст"\` или одинарные \`'текст'\`. Результат одинаковый.

2. **Каждый print() — новая строка.** После вывода Python автоматически переходит на следующую строку.

3. **Можно выводить числа без кавычек:**
\`\`\`python
print(42)
print(3.14)
\`\`\`

4. **Можно выводить несколько значений через запятую:**
\`\`\`python
print("Привет", "мир")    # Привет мир
print("Возраст:", 25)     # Возраст: 25
\`\`\`
Python добавляет пробел между значениями автоматически.

5. **Пустой print() — просто переход на новую строку:**
\`\`\`python
print("Первая строка")
print()
print("После пустой строки")
\`\`\`

### Частые ошибки

- Забыли кавычки: \`print(привет)\` — ошибка! Python думает, что \`привет\` — это переменная.
- Забыли скобки: \`print "привет"\` — ошибка! В Python 3 скобки обязательны.
- Не закрыли кавычку: \`print("привет)\` — ошибка!

## Команда input()

\`input()\` — читает текст, который вводит пользователь.

### Синтаксис

\`\`\`python
name = input()
\`\`\`

Что происходит:
1. Программа останавливается и ждёт, пока пользователь напечатает что-то и нажмёт Enter
2. Введённый текст сохраняется в переменную \`name\`
3. Программа продолжает работу

### Переменные

Переменная — это имя для значения. Как наклейка на коробке.

\`\`\`python
name = "Анна"      # создали переменную name со значением "Анна"
age = 25           # создали переменную age со значением 25
print(name)        # выведет: Анна
print(age)         # выведет: 25
\`\`\`

Правила именования:
- Только буквы, цифры и подчёркивание: \`my_name\`, \`age2\`
- Нельзя начинать с цифры: \`2name\` — ошибка!
- Нельзя использовать пробелы: \`my name\` — ошибка! Используйте \`my_name\`
- Python различает регистр: \`Name\` и \`name\` — разные переменные

### f-строки

f-строки — удобный способ вставить переменную в текст:

\`\`\`python
name = "Анна"
age = 25
print(f"Привет, {name}! Тебе {age} лет.")
# Привет, Анна! Тебе 25 лет.
\`\`\`

Правила:
- Буква \`f\` перед открывающей кавычкой
- Переменные в \`{фигурных скобках}\`
- Внутри \`{}\` можно даже считать: \`f"Два плюс три = {2 + 3}"\`

### Полный пример

\`\`\`python
# Программа "Визитка"
name = input()                          # читаем имя
city = input()                          # читаем город
print(f"Привет! Меня зовут {name}.")
print(f"Я живу в городе {city}.")
print("Приятно познакомиться!")
\`\`\`

## Комментарии

Строки, начинающиеся с \`#\`, игнорируются Python. Они для людей:

\`\`\`python
# Это комментарий — Python его не выполняет
print("А это выполняет")  # комментарий в конце строки тоже ок
\`\`\`

Используйте комментарии, чтобы объяснить сложные места в коде.`,
  },

  1: {
    title: "Модуль 1: Переменные и типы — полный учебник",
    content: `# Переменные и типы данных

## Типы данных в Python

Python работает с разными видами данных. Три основных типа:

### int — целые числа

\`\`\`python
age = 25
temperature = -10
year = 2025
population = 8000000000   # 8 миллиардов
\`\`\`

Целые числа не имеют дробной части. Могут быть положительными, отрицательными или нулём.

### float — дробные числа

\`\`\`python
pi = 3.14159
temperature = 36.6
price = 19.99
\`\`\`

Обратите внимание: в Python используется точка, а не запятая: \`3.14\`, не \`3,14\`.

### str — строки (текст)

\`\`\`python
name = "Анна"
greeting = 'Привет!'
empty = ""              # пустая строка тоже строка
\`\`\`

## Арифметические операции

\`\`\`python
# Основные операции
print(10 + 3)    # 13  (сложение)
print(10 - 3)    # 7   (вычитание)
print(10 * 3)    # 30  (умножение)
print(10 / 3)    # 3.333... (деление, всегда float!)
print(10 // 3)   # 3   (целочисленное деление, округление вниз)
print(10 % 3)    # 1   (остаток от деления)
print(2 ** 10)   # 1024 (возведение в степень)
\`\`\`

### Приоритет операций

Как в математике: сначала **, потом * / // %, потом + -

\`\`\`python
print(2 + 3 * 4)     # 14 (не 20!)
print((2 + 3) * 4)   # 20 (скобки меняют порядок)
\`\`\`

## Преобразование типов

\`input()\` всегда возвращает строку. Чтобы работать с числами:

\`\`\`python
text = input()       # пользователь ввёл "25"
print(type(text))    # <class 'str'> — это строка!

number = int(text)   # преобразовали в целое число
print(type(number))  # <class 'int'>

decimal = float(text)  # преобразовали в дробное
print(type(decimal))   # <class 'float'>
\`\`\`

Короткая запись:
\`\`\`python
age = int(input())     # прочитать и сразу преобразовать
price = float(input())
\`\`\`

### Ошибки преобразования

\`\`\`python
int("abc")      # ValueError! Нельзя преобразовать буквы в число
int("3.14")     # ValueError! int() не понимает дробные строки
float("3.14")   # 3.14 — float() может
int(3.14)       # 3 — отбросит дробную часть (не округлит!)
\`\`\`

## Строковые операции

\`\`\`python
# Длина строки
name = "Python"
print(len(name))    # 6

# Доступ по индексу (нумерация с 0!)
print(name[0])   # P
print(name[1])   # y
print(name[-1])  # n (последний)

# Конкатенация (склейка)
first = "Hello"
second = "World"
print(first + " " + second)   # Hello World

# Повторение
print("ha" * 3)   # hahaha

# Методы
print("hello".upper())       # HELLO
print("HELLO".lower())       # hello
print("  hello  ".strip())   # hello (убрать пробелы по краям)
print("hello".replace("l", "r"))  # herro
print("hello".count("l"))    # 2
\`\`\`

## Присваивание

\`\`\`python
x = 10       # x теперь 10
x = x + 5    # x теперь 15
x += 5       # то же самое, короче. x теперь 20
x -= 3       # x теперь 17
x *= 2       # x теперь 34
x //= 10     # x теперь 3
\`\`\`

Операторы \`+=\`, \`-=\`, \`*=\`, \`/=\`, \`//=\`, \`%=\` — сокращённая запись.`,
  },

  2: {
    title: "Модуль 2: Условия — полный учебник",
    content: `# Условные конструкции

## Логика: True и False

В Python есть тип данных \`bool\` — логический. Он принимает только два значения:
- \`True\` (истина)
- \`False\` (ложь)

\`\`\`python
print(5 > 3)     # True
print(5 < 3)     # False
print(5 == 5)    # True
print(5 != 5)    # False
\`\`\`

## Операторы сравнения

| Оператор | Значение | Пример | Результат |
|----------|----------|--------|-----------|
| == | равно | 5 == 5 | True |
| != | не равно | 5 != 3 | True |
| > | больше | 5 > 3 | True |
| < | меньше | 5 < 3 | False |
| >= | больше или равно | 5 >= 5 | True |
| <= | меньше или равно | 5 <= 3 | False |

Важно: \`=\` (одно равно) — присваивание, \`==\` (два равно) — сравнение!

## Конструкция if

\`\`\`python
age = int(input())
if age >= 18:
    print("Вы совершеннолетний")
\`\`\`

Правила:
1. После \`if\` — условие
2. В конце строки — двоеточие \`:\`
3. Код внутри \`if\` — с отступом в 4 пробела
4. Код выполняется только если условие True

## if-else

\`\`\`python
age = int(input())
if age >= 18:
    print("Доступ разрешён")
else:
    print("Доступ запрещён")
\`\`\`

Всегда выполняется ровно одна ветка: либо \`if\`, либо \`else\`.

## if-elif-else

Для нескольких вариантов:

\`\`\`python
score = int(input())
if score >= 90:
    print("Отлично")
elif score >= 70:
    print("Хорошо")
elif score >= 50:
    print("Удовлетворительно")
else:
    print("Неудовлетворительно")
\`\`\`

Python проверяет условия сверху вниз и выполняет первое подходящее. Остальные пропускает.

## Логические операторы

\`\`\`python
# and — оба условия должны быть True
if age >= 18 and age <= 65:
    print("Рабочий возраст")

# or — хотя бы одно условие True
if day == "суббота" or day == "воскресенье":
    print("Выходной")

# not — инвертирует
if not is_banned:
    print("Доступ есть")
\`\`\`

Таблица истинности:
- True and True = True
- True and False = False
- True or False = True
- False or False = False
- not True = False

## Вложенные условия

\`\`\`python
has_ticket = True
age = 15

if has_ticket:
    if age >= 18:
        print("Проходите")
    else:
        print("Нужно сопровождение взрослого")
else:
    print("Купите билет")
\`\`\`

## Проверка делимости

Оператор \`%\` (остаток от деления) — ключ к проверке делимости:

\`\`\`python
n = int(input())

if n % 2 == 0:
    print("Чётное")
else:
    print("Нечётное")

if n % 10 == 0:
    print("Делится на 10")

if n % 3 == 0 and n % 5 == 0:
    print("Делится на 15")
\`\`\``,
  },

  3: {
    title: "Модуль 3: Циклы и списки — полный учебник",
    content: `# Циклы и списки

## Зачем нужны циклы

Представьте, что нужно вывести числа от 1 до 100. Без цикла — 100 строк кода. С циклом — 3 строки.

## Цикл while

\`while\` повторяет код, пока условие истинно:

\`\`\`python
i = 1
while i <= 5:
    print(i)
    i += 1
\`\`\`

Как работает:
1. Проверяется условие i <= 5 → True (i=1)
2. Выполняется тело: print(1), i становится 2
3. Проверяется условие i <= 5 → True (i=2)
4. ... повторяется ...
5. Проверяется условие i <= 5 → False (i=6)
6. Цикл завершается

**Важно:** если забыть \`i += 1\`, цикл будет бесконечным!

### break и continue

\`\`\`python
# break — немедленный выход из цикла
while True:
    cmd = input()
    if cmd == "выход":
        break       # выходим из цикла
    print(f"Вы ввели: {cmd}")

# continue — пропустить текущую итерацию
i = 0
while i < 10:
    i += 1
    if i % 2 == 0:
        continue    # пропускаем чётные
    print(i)        # выведет: 1, 3, 5, 7, 9
\`\`\`

## Цикл for

\`for\` — удобнее для перебора последовательностей:

\`\`\`python
# Перебор range
for i in range(5):      # 0, 1, 2, 3, 4
    print(i)

# Перебор строки
for char in "Hello":    # H, e, l, l, o
    print(char)

# Перебор списка
for name in ["Анна", "Пётр", "Мир"]:
    print(name)
\`\`\`

### range() подробно

\`\`\`python
range(5)         # 0, 1, 2, 3, 4
range(1, 6)      # 1, 2, 3, 4, 5
range(0, 10, 2)  # 0, 2, 4, 6, 8
range(10, 0, -1) # 10, 9, 8, ..., 1
\`\`\`

## Списки (list)

Список — упорядоченная коллекция элементов:

\`\`\`python
# Создание
nums = [1, 2, 3, 4, 5]
names = ["Анна", "Пётр"]
mixed = [1, "hello", True, 3.14]
empty = []

# Доступ по индексу
print(nums[0])    # 1 (первый)
print(nums[-1])   # 5 (последний)
print(nums[1:3])  # [2, 3] (срез)

# Изменение
nums[0] = 10      # [10, 2, 3, 4, 5]

# Добавление
nums.append(6)    # [10, 2, 3, 4, 5, 6]
nums.insert(0, 0) # [0, 10, 2, 3, 4, 5, 6]

# Удаление
nums.remove(10)   # удалить первое вхождение 10
nums.pop()        # удалить и вернуть последний
del nums[0]       # удалить по индексу

# Полезные функции
print(len(nums))      # длина
print(sum(nums))      # сумма
print(max(nums))      # максимум
print(min(nums))      # минимум
print(sorted(nums))   # отсортированная копия
nums.sort()           # сортировка на месте
nums.reverse()        # переворот на месте
\`\`\`

### Перебор списка с индексом

\`\`\`python
names = ["Анна", "Пётр", "Мир"]

# С помощью enumerate
for i, name in enumerate(names):
    print(f"{i}: {name}")
# 0: Анна
# 1: Пётр
# 2: Мир

# С помощью enumerate и стартовым номером
for i, name in enumerate(names, 1):
    print(f"{i}. {name}")
# 1. Анна
# 2. Пётр
# 3. Мир
\`\`\`

### Проверка наличия

\`\`\`python
nums = [1, 2, 3, 4, 5]
print(3 in nums)      # True
print(10 in nums)     # False
print(10 not in nums) # True
\`\`\``,
  },

  4: {
    title: "Модуль 4: Функции — полный учебник",
    content: `# Функции

## Зачем нужны функции

Функции позволяют:
1. **Не повторять код** — написал один раз, вызываешь сколько угодно
2. **Разбить задачу на части** — каждая функция решает одну маленькую задачу
3. **Сделать код читаемым** — имя функции описывает что она делает

## Создание функции

\`\`\`python
def greet(name):
    print(f"Привет, {name}!")

# Вызов
greet("Анна")     # Привет, Анна!
greet("Пётр")     # Привет, Пётр!
\`\`\`

- \`def\` — ключевое слово (define — определить)
- \`greet\` — имя функции
- \`(name)\` — параметр (входные данные)
- Двоеточие \`:\` в конце строки
- Тело функции с отступом в 4 пробела

## return — возврат значения

\`\`\`python
def add(a, b):
    return a + b

result = add(3, 5)    # result = 8
print(add(10, 20))    # 30
\`\`\`

- \`return\` возвращает результат туда, где функция была вызвана
- После \`return\` функция прекращает работу
- Функция без \`return\` возвращает \`None\`

## Несколько параметров

\`\`\`python
def full_name(first, last):
    return f"{first} {last}"

print(full_name("Анна", "Иванова"))   # Анна Иванова
\`\`\`

## Параметры по умолчанию

\`\`\`python
def greet(name, greeting="Привет"):
    return f"{greeting}, {name}!"

print(greet("Анна"))                    # Привет, Анна!
print(greet("Анна", "Здравствуйте"))   # Здравствуйте, Анна!
\`\`\`

## Локальные и глобальные переменные

\`\`\`python
x = 10                # глобальная переменная

def foo():
    x = 20            # локальная переменная (другая!)
    print(x)          # 20

foo()
print(x)              # 10 — глобальная не изменилась
\`\`\`

Переменные внутри функции — локальные. Они существуют только пока функция работает.

## Функция может вызывать другие функции

\`\`\`python
def square(n):
    return n * n

def sum_of_squares(a, b):
    return square(a) + square(b)

print(sum_of_squares(3, 4))   # 25 (9 + 16)
\`\`\`

## Чистые функции

Хорошая функция:
- Получает данные через параметры
- Возвращает результат через return
- Не меняет ничего снаружи

\`\`\`python
# Хорошо:
def double(n):
    return n * 2

# Плохо:
result = 0
def bad_double(n):
    global result        # меняет глобальную переменную
    result = n * 2
\`\`\``,
  },

  15: {
    title: "Модуль 15-19: ООП — полный учебник",
    content: `# Объектно-ориентированное программирование

## Что такое ООП

ООП — способ организации кода вокруг «объектов». Объект — это данные + функции для работы с ними.

Аналогия: автомобиль — это объект.
- Данные (атрибуты): цвет, скорость, бензин
- Функции (методы): ехать(), тормозить(), заправить()

## Класс и объект

**Класс** — чертёж (шаблон). **Объект** — конкретная вещь по этому чертежу.

\`\`\`python
class Car:
    def __init__(self, brand, color):
        self.brand = brand
        self.color = color
        self.speed = 0

    def accelerate(self, amount):
        self.speed += amount

    def brake(self):
        self.speed = 0

    def __str__(self):
        return f"{self.color} {self.brand}, скорость: {self.speed}"

# Создаём объекты
car1 = Car("Toyota", "красная")
car2 = Car("BMW", "чёрная")

car1.accelerate(60)
print(car1)   # красная Toyota, скорость: 60
print(car2)   # чёрная BMW, скорость: 0
\`\`\`

## __init__ — конструктор

Вызывается автоматически при создании объекта. Здесь задаются начальные значения.

\`self\` — ссылка на сам объект. Через \`self.xxx\` сохраняем данные внутри объекта.

## Наследование

Создание нового класса на основе существующего:

\`\`\`python
class Animal:
    def __init__(self, name):
        self.name = name
    def speak(self):
        pass

class Dog(Animal):
    def speak(self):
        return f"{self.name}: Гав!"

class Cat(Animal):
    def speak(self):
        return f"{self.name}: Мяу!"
\`\`\`

## super()

Вызов метода родительского класса:

\`\`\`python
class Employee:
    def __init__(self, name, salary):
        self.name = name
        self.salary = salary

class Manager(Employee):
    def __init__(self, name, salary, team_size):
        super().__init__(name, salary)    # вызываем Employee.__init__
        self.team_size = team_size
\`\`\`

## Инкапсуляция

Скрытие внутренних данных:

\`\`\`python
class BankAccount:
    def __init__(self, balance):
        self._balance = balance      # «приватный» атрибут

    def deposit(self, amount):
        if amount > 0:
            self._balance += amount

    def withdraw(self, amount):
        if 0 < amount <= self._balance:
            self._balance -= amount
            return True
        return False

    @property
    def balance(self):
        return self._balance
\`\`\`

## @property

Делает метод похожим на атрибут:

\`\`\`python
class Circle:
    def __init__(self, radius):
        self._radius = radius

    @property
    def area(self):
        return 3.14159 * self._radius ** 2

c = Circle(5)
print(c.area)   # 78.5... — вызывается как атрибут, не как метод
\`\`\`

## @staticmethod и @classmethod

\`\`\`python
class Math:
    @staticmethod
    def add(a, b):          # не нужен self
        return a + b

    @classmethod
    def from_string(cls, s):  # получает класс
        return cls()

print(Math.add(3, 5))   # 8 — вызов без создания объекта
\`\`\`

## Принципы ООП

1. **Инкапсуляция** — скрывайте данные, давайте доступ через методы
2. **Наследование** — создавайте новые классы на основе существующих
3. **Полиморфизм** — один интерфейс, разное поведение
4. **Абстракция** — показывайте только важное, скрывайте детали`,
  },
};

// Для модулей без отдельного учебника — сгенерируем заглушку
export function getTextbook(module: number): { title: string; content: string } | null {
  return textbook[module] || null;
}
